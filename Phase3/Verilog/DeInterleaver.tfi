


// Instantiate the module
DeInterleaver instance_name (
    .Clk(Clk), 
    .Reset(Reset), 
    .Start(Start), 
    .x(x), 
    .y(y), 
    .Rate(Rate), 
    .Valid(Valid), 
    .Ncbps(Ncbps)
    );


